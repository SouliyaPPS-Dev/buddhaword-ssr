// Define a type or interface for a single object structure
export interface VideoDataModel {
     ID: string;
     ຊື່ພຣະສູດ: string;
     ພຣະສູດ: string;
     ໝວດທັມ: string;
     link: string;
}

// Define the array type
export type VideoDataArray = VideoDataModel[];
